当 全局 statement 有 vec.pop 调用时, 有LLVM IR错误:
[codegen] module verification failed:
Terminator found in the middle of a basic block!
label %sf.skip0

比如程序:

vec(string) vec_str
vec_str.push("123")

print(vec_str[0])

vec_str.pop()

fn main() -> int {
    print("main")
}

