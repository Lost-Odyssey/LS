关键字 in 做为函数参数时 报错, 编译器需要提供更有意思的 error 说明;
或者允许变量名可与 关键字 重叠.


fn string_op(string in) {
    string out = in + "_test"
    print(out)
}

fn main() -> int {
    string s = "static"
    string_op(s.upper())
    print(s)
    return 0
}